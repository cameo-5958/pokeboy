// Synchronous host imports exposed to .gbmod packages running in the browser.
// Add trusted functions here using the documented (cpu, memory) signature.
// Async functions are intentionally unsupported because the emulated CPU is
// suspended inside a host-call trap until the function returns.
window.PokeboyModCore = Object.freeze({});
