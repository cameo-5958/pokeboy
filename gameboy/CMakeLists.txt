cmake_minimum_required(VERSION 3.20)
project(gbemu CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_library(gbcore
    core/gb.cpp
    core/bus/bus.cpp
    core/cpu/cpu.cpp
    core/ppu/ppu.cpp
    core/timer/timer.cpp
    core/apu/apu.cpp
    core/cart/cart.cpp
    adapter/gb_api.cpp)
target_include_directories(gbcore PUBLIC core adapter)
add_subdirectory(../pkai pkai)
target_link_libraries(gbcore PUBLIC pkai)

# Per-device frontends live in the repo-root emulator/<device>/ tree; this
# project builds the core they all link against.
add_executable(gbemu_headless ../emulator/headless/headless.cpp)
target_link_libraries(gbemu_headless PRIVATE gbcore)

include(CTest)
if(BUILD_TESTING)
    add_executable(gbemu_boot_tests tests/boot_tests.cpp)
    target_link_libraries(gbemu_boot_tests PRIVATE gbcore)
    add_test(NAME custom_boot_rom COMMAND gbemu_boot_tests)

    add_executable(gbcore_state_tests tests/state_tests.cpp)
    target_link_libraries(gbcore_state_tests PRIVATE gbcore)
    add_test(NAME gbcore_state_tests COMMAND gbcore_state_tests)

    add_executable(gbcore_hardware_tests tests/hardware_tests.cpp)
    target_link_libraries(gbcore_hardware_tests PRIVATE gbcore)
    add_test(NAME gbcore_hardware_tests COMMAND gbcore_hardware_tests)

endif()

option(POKEBOY_LINUX_FRONTEND "Build the Linux fbdev/evdev/tinyalsa device frontend" ON)
if(POKEBOY_LINUX_FRONTEND AND CMAKE_SYSTEM_NAME STREQUAL "Linux" AND NOT EMSCRIPTEN)
    add_executable(gbemu_linux ../emulator/linux/main_linux.cpp)
    target_link_libraries(gbemu_linux PRIVATE gbcore)
    # tinyalsa: the system copy when there is one (Buildroot's staging tree on the
    # device build), otherwise fetched and built in-tree so a plain Linux host
    # gets the audio backend too.
    find_library(TINYALSA_LIB tinyalsa)
    find_path(TINYALSA_INC tinyalsa/pcm.h)
    if(TINYALSA_LIB AND TINYALSA_INC)
        target_include_directories(gbemu_linux PRIVATE ${TINYALSA_INC})
        target_link_libraries(gbemu_linux PRIVATE ${TINYALSA_LIB})
    else()
        include(FetchContent)
        FetchContent_Declare(tinyalsa
            GIT_REPOSITORY https://github.com/tinyalsa/tinyalsa.git
            GIT_TAG v2.0.0
            GIT_SHALLOW TRUE)
        set(CMAKE_POLICY_DEFAULT_CMP0077 NEW)
        set(CMAKE_POLICY_VERSION_MINIMUM 3.5)   # tinyalsa still says 3.1, which CMake 4 rejects
        set(BUILD_SHARED_LIBS OFF)
        set(TINYALSA_BUILD_EXAMPLES OFF)
        set(TINYALSA_BUILD_UTILS OFF)
        FetchContent_MakeAvailable(tinyalsa)
        # Upstream builds with -Werror; newer GCCs flag its strncpy use.
        target_compile_options(tinyalsa PRIVATE -Wno-error)
        target_link_libraries(gbemu_linux PRIVATE tinyalsa)
    endif()
    target_compile_definitions(gbemu_linux PRIVATE POKEBOY_TINYALSA=1)
    install(TARGETS gbemu_linux RUNTIME DESTINATION bin)
endif()

if(WIN32)
    add_executable(gbemu WIN32 ../emulator/win32/main_win32.cpp)
    target_link_libraries(gbemu PRIVATE gbcore user32 gdi32 winmm comdlg32)
    set_target_properties(gbemu PROPERTIES LINK_FLAGS "/ENTRY:mainCRTStartup")
endif()

# Dumps every enemy trainer party by executing the ROM's ReadTrainer (see
# pkai/tools/trainer_dump.h); ai/tools/dump_trainers.py drives it.
add_executable(pkai_dump_trainers ../pkai/tools/dump_trainers.cpp)
target_link_libraries(pkai_dump_trainers PRIVATE gbcore)
target_compile_definitions(pkai_dump_trainers PRIVATE
    AI_ROM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc"
    AI_SYM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.sym")

if(BUILD_TESTING)
    add_executable(pkai_protocol_tests ../pkai/tests/protocol_tests.cpp)
    target_link_libraries(pkai_protocol_tests PRIVATE gbcore)
    target_compile_definitions(pkai_protocol_tests PRIVATE AI_ROM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc")
    add_test(NAME pkai_protocol COMMAND pkai_protocol_tests)
    set_tests_properties(pkai_protocol PROPERTIES SKIP_RETURN_CODE 77)
endif()
if(BUILD_TESTING)
    # Trained PEP checkpoint (gitignored): pkai_model_tests needs pkai.weights + vectors/bin,
    # pkai_rom_battles runs a model-backed battle when pkai.weights is present.
    set(PKAI_CHECKPOINT ${CMAKE_CURRENT_SOURCE_DIR}/../ai/checkpoints/pep/current CACHE PATH "PEP checkpoint used by the pkai tests")
    add_executable(pkai_rom_battle_tests ../pkai/tests/rom_battle_tests.cpp)
    target_link_libraries(pkai_rom_battle_tests PRIVATE gbcore)
    target_compile_definitions(pkai_rom_battle_tests PRIVATE
        AI_ROM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc"
        AI_SYM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.sym"
        AI_WEIGHTS="${PKAI_CHECKPOINT}/pkai.weights")
    add_test(NAME pkai_rom_battles COMMAND pkai_rom_battle_tests)
    set_tests_properties(pkai_rom_battles PROPERTIES TIMEOUT 120 SKIP_RETURN_CODE 77)
    # Event vector: integer quantisation vs a table generated by ai/models/pep_int.py.
    add_executable(pkai_event_tests ../pkai/tests/event_tests.cpp)
    target_link_libraries(pkai_event_tests PRIVATE pkai)
    add_test(NAME pkai_event_tests COMMAND pkai_event_tests)
    add_executable(pkai_calc_tests ../pkai/tests/calc_tests.cpp)
    target_link_libraries(pkai_calc_tests PRIVATE gbcore)
    target_compile_definitions(pkai_calc_tests PRIVATE AI_ROM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc")
    add_test(NAME pkai_calc COMMAND pkai_calc_tests)
    set_tests_properties(pkai_calc PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(pkai_trainer_tests ../pkai/tests/trainer_tests.cpp)
    target_link_libraries(pkai_trainer_tests PRIVATE gbcore)
    target_compile_definitions(pkai_trainer_tests PRIVATE
        AI_ROM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc"
        AI_SYM="${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.sym")
    add_test(NAME pkai_trainers COMMAND pkai_trainer_tests)
    set_tests_properties(pkai_trainers PROPERTIES SKIP_RETURN_CODE 77)
    # Integer model vs ai/models/pep_int.py reference vectors (skips if the
    # gitignored checkpoint/vectors are absent; see pkai/CMakeLists.txt pkai_vectors).
    add_executable(pkai_model_tests ../pkai/tests/model_tests.cpp)
    target_link_libraries(pkai_model_tests PRIVATE pkai)
    add_test(NAME pkai_model_tests COMMAND pkai_model_tests ${PKAI_CHECKPOINT}/pkai.weights ${PKAI_CHECKPOINT}/vectors/bin)
    set_tests_properties(pkai_model_tests PROPERTIES SKIP_RETURN_CODE 77)
    find_package(Python3 COMPONENTS Interpreter)
    if(Python3_Interpreter_FOUND)
        add_test(NAME pkai_python_binding COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/../pkai/tests/test_python_binding.py
                 ${CMAKE_BINARY_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../pred-patch/pokered-ai.gbc)
        set_tests_properties(pkai_python_binding PROPERTIES DEPENDS pkai_rom_battles SKIP_RETURN_CODE 77)
    endif()
endif()
